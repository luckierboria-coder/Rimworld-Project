name: Build RimMT V0.9.10 Source-Reconciled Continuation

on:
  push:
    branches: [ rimmt-v0.9.2-unified-lean ]
    paths:
      - 'RimMT/**'
      - '.github/workflows/build-rimmt-v092-unified.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Apply V22 Stable transforms
        shell: pwsh
        run: ./RimMT/Build/ApplyUnifiedLeanTransforms.ps1

      - name: Apply V23 S4 pruners
        shell: pwsh
        run: ./RimMT/Build/ApplyUnifiedLeanV23S4Pruners.ps1

      - name: Apply V24 S4 telemetry and pruners
        shell: pwsh
        run: ./RimMT/Build/ApplyUnifiedLeanV24S4TelemetryAndPruners.ps1

      - name: Apply consolidated stable core marker
        shell: pwsh
        run: ./RimMT/Build/ApplyUnifiedLeanV26Consolidated.ps1

      - name: Apply RimMT V0.9.4 Tail Focus base
        shell: pwsh
        run: ./RimMT/Build/ApplyRimMTV094TailFocus.ps1

      - name: Apply RimMT V0.9.5 Resumable JobGiver
        shell: pwsh
        run: ./RimMT/Build/ApplyRimMTV095ResumableJobGiver.ps1

      - name: Apply RimMT V0.9.6 Continuation Fix
        shell: pwsh
        run: ./RimMT/Build/ApplyRimMTV096ContinuationFix.ps1

      - name: Apply RimMT V0.9.7 Continuation Deferral
        shell: pwsh
        run: ./RimMT/Build/ApplyRimMTV097ContinuationDeferral.ps1

      - name: Apply RimMT V0.9.8 Logical Scanner Identity
        shell: pwsh
        run: ./RimMT/Build/ApplyRimMTV098LogicalScannerIdentity.ps1

      - name: Apply RimMT V0.9.9 Package Context Continuation
        shell: pwsh
        run: ./RimMT/Build/ApplyRimMTV099PackageContextContinuation.ps1

      - name: Apply RimMT V0.9.10 Source-Reconciled Continuation
        shell: pwsh
        run: ./RimMT/Build/ApplyRimMTV0910SourceReconciledContinuation.ps1

      - name: Restore
        run: dotnet restore RimMT/Source/RimMT/RimMT.csproj

      - name: Build Release
        run: dotnet build RimMT/Source/RimMT/RimMT.csproj --configuration Release --no-restore

      - name: Verify single DLL
        shell: pwsh
        run: |
          $dir = 'RimMT/1.5/Assemblies'
          $dll = Join-Path $dir 'RimMT.dll'
          if (!(Test-Path $dll)) { throw "Missing compiled assembly: $dll" }
          $all = @(Get-ChildItem $dir -Filter '*.dll' -File)
          if ($all.Count -ne 1) { throw "Production build must contain exactly one DLL; found $($all.Count): $($all.Name -join ', ')" }
          Get-Item $dll | Format-List FullName,Length,LastWriteTime

      - name: Package
        shell: pwsh
        run: |
          $stage = 'build/RimMT'
          New-Item -ItemType Directory -Force -Path $stage | Out-Null
          Copy-Item 'RimMT/About' $stage -Recurse
          Copy-Item 'RimMT/Languages' $stage -Recurse
          Copy-Item 'RimMT/1.5' $stage -Recurse
          Copy-Item 'RimMT/LoadFolders.xml' $stage
          if (Test-Path 'RimMT/README.md') { Copy-Item 'RimMT/README.md' $stage }
          $packDlls = @(Get-ChildItem $stage -Filter '*.dll' -File -Recurse)
          if ($packDlls.Count -ne 1 -or $packDlls[0].Name -ne 'RimMT.dll') {
            throw "Package contains unexpected DLL set: $($packDlls.Name -join ', ')"
          }
          Compress-Archive -Path $stage -DestinationPath 'build/RimMT_V0.9.10_Source_Reconciled_Continuation.zip' -Force

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: RimMT-V0.9.10-Source-Reconciled-Continuation
          path: build/RimMT_V0.9.10_Source_Reconciled_Continuation.zip
          if-no-files-found: error
